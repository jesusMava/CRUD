<p id="notice"><%= notice %></p>

<h1>Contactos</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Telefono</th>
      <th>Email</th>
      <th>Dirección</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contactos.each do |contacto| %>
      <tr>
        <td><%= contacto.name %></td>
        <td><%= contacto.tel %></td>
        <td><%= contacto.email %></td>
        <td><%= contacto.address %></td>
        <td><%= link_to 'Mostrar', contacto %></td>
        <td><%= link_to 'Editar', edit_contacto_path(contacto) %></td>
        <td><%= link_to 'Eliminar', contacto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Contacto', new_contacto_path %>
